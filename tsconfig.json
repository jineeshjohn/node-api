{
  "compilerOptions": {
    // …your existing options…

    /*  A  — don’t halt on errors, still write .js files  */
    "noEmitOnError": false,

    /*  B  — skip type-checking of all .d.ts files that come with libs */
    "skipLibCheck": true
  }
}
